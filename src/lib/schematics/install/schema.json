{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsFlexLayoutInstall",
  "title": "Flex Layout Install Options Schema",
  "type": "object",
  "properties": {
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Do not add Flex Layout dependencies to package.json (e.g., --skipPackageJson)"
    }
  },
  "required": []
}
