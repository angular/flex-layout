import { SplitAreaDirective } from './split-area.directive';

describe('SplitAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitAreaDirective();
    expect(directive).toBeTruthy();
  });
});
