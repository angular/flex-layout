import { SplitHandleDirective } from './split-handle.directive';

describe('SplitHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitHandleDirective();
    expect(directive).toBeTruthy();
  });
});
