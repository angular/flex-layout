import { SplitDirective } from './split.directive';

describe('SplitDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitDirective();
    expect(directive).toBeTruthy();
  });
});
